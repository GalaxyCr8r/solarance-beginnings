# Overview

This folder should ONLY contain type definitions (structs, enums, etc.) and utility functions directly related to them. (e.g. impls, utility functions that aren't needed elsewhere)

All gameplay logic should go in the `logic/` folder.
